services:
  llm-bot:
    build: .
    container_name: llm-bot
    volumes:
      - ./state:/app/state
      - ./crontab:/app/crontab  # Mount crontab for easy editing
    env_file:
      - .env
    restart: unless-stopped
    # Cron-based scheduling - edit crontab file to change schedule